# Copy to .env.local (Vite auto-loads it, and it's gitignored by
# Vite's default .gitignore) if your backend isn't running on the
# default http://localhost:4000
VITE_API_URL=http://localhost:4000/api
